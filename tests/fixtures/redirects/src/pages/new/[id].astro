---
export const prerender = true;

export function getStaticPaths() {
  return [{ params: { id: "a" } }, { params: { id: "b" } }];
}

const { id } = Astro.params;
---

<html>
  <body><p id="new-page">{id}</p></body>
</html>
